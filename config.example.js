const config = {
  port: 3001,
  site_url: "https://example.com/", // website url, "/" required in end of url 
  auth: "your-secret-auth-token" // authorization token - change this to a secure token
};

export default config;